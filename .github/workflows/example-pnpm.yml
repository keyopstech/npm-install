name: example-pnpm
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2

      - uses: keyopstech/install-deps-action@v2

      # - name: 'Test: install'
      #   run: pnpm install
      - uses: ./
        with:
          usePnpm: true
          working-directory: examples/pnpm
      # - run: npm t
      #   working-directory: examples/pnpm
